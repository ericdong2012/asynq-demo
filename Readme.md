`
# environment
```
docker network create asynq-demo_looklook_net

docker-compose up -d

```

# desc
```

server/server.go  has the server and client code
server/server2.go has the other server 
In this scenario, always retry and report error: handler not found will appear

```

`